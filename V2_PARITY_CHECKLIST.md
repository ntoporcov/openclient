# V2 Parity Checklist

September 9, 2026: the user approved **public automatic detection by default and only, with no API picker**, superseding Legacy-default and explicit Experimental/v2 opt-in instructions. Experimental "mostly works" remains the confidence goal, not full parity or release certification. The remote rollout flag stays Track Later, separately from this approved default change; no remote flag implementation is required. Final source verification retains its explicit exclusion and combined-run failure. The fresh automatic-detection build is now installed on Nic iPhone; launch failed because the phone was locked.

**Status:** Implemented = code path exists, remaining verification stated; Verified = stated behavior passed the specified tests, not universal parity; Partial = some behavior works but gaps remain; Blocked = unavailable pending a contract or prerequisite; Deferred = explicit user decision, not a current blocker. Evidence details and historical results live in [the adoption guide](OPENCODE_V2_ADOPTION_PLAN.md); service boundaries live in [Backend Injection](BACKEND_INJECTION.md).

| Surface | Status | Current boundary / next concrete step |
| --- | --- | --- |
| Connection negotiation and core backend injection | Verified | Public Automatic only; saved `legacy`/`v2`/missing preferences migrate to `automatic`. Explicit internal/test overrides remain factory-captured. Valid `GET /api/health` selects v2; only unavailable 404/405/successful HTML/recognized minimal legacy health permits `/global/health` fallback. 401/403, timeout/network/TLS, other errors and malformed/unhealthy responses fail closed. Resolved API is fixed for the connection lifetime; no mutation retries. Final units and three automatic UI passes per device recorded below; no second-harness target. |
| Saved credentials and public loader | Verified | Main app and headless Shortcuts use the public saved-server loader. Plaintext-to-Keychain migration requires verified readback; existing Keychain values, including blank, win. Deferred failure retains original raw bytes and centrally blocks metadata/save/edit/delete/remember writes until user retry resolves the secure-storage error. Raw `serverID`/Keychain identity stays unchanged; final source-unit results include migration coverage, not actual Shortcuts-host verification. |
| Historical profile ownership | Implemented | Widget, Live Activity, window and cache profile IDs remain unchanged; absent historical profile remains legacy. Public preference migration does not retype records, add a migrated-record type or change legacy cache/schema identities. |
| Experimental v2 notice | Implemented | Nonblocking banner after successful bootstrap for actual bound backend UUID; hidden during loading/overlays or inactive connection state. Dismiss per connection, no persistent acknowledgement, no SSE dismissal reset. Global Forms have priority; banner never gates chat. Report a Bug and Help share the canonical bare Support URL `https://github.com/ntoporcov/openclient/issues`. Source keys/translations included; main confirmed current lint passed for 1,492 entries/seven catalogs. |
| Projects, sessions, Home/Activity/search | Verified | Core navigation, scoped pagination, previews and lifecycle tests; earlier UI results remain historical. Continue physical-device checks. |
| Chat, streaming, composer, permissions | Partial | Five-turn normal-mode native composer acceptance passes on both required simulators in separate actual-next-17155 runs. Final combined-run iPhone failed in Photo Picker with no selected item/Done disabled; not a clean combined matrix. Initial-bottom positioning now retries after usable layout, handles token only on success and cancels on user drag; 13 focused passes each. Normal cold relaunch shows latest canonical content, five user/five assistant messages, without test scroll/refresh. Not paid inference or hands-on permission evidence. |
| General submission recovery (Legacy and v2) | Verified | The user's actual issue was Legacy. Retained payload is separate from canonical transcript; queued/uncertain cards GET status only, never auto-POST. Same raw-server/profile owner reconnect retains unknown text/files/mentions/original ID in process memory; foreign scopes isolated. Exact canonical evidence overrides old local prefixes without optimistic/cache/widget-ledger false confirmation. Seeded root/dedicated-window UI covers both profiles; real-adapter mocked-408 regressions pass. No app-kill recovery persistence or new database. |
| Foreground active-chat refresh | Verified | Coalescer/activation/latest-request guards pass slow/out-of-order tests. Earlier UI uses a snapshot proxy; latest normal-mode core acceptance also verifies actual-runtime background completion and foreground catch-up without pull. Not physical background or ActivityKit restoration evidence. |
| Native session forms | Verified | Shared engine, typed submit/cancel, optional/default/conditional fields, custom multiselect and explicit external acknowledgement. HTTP/event and native UI tests pass; unknown schemas stay unsupported, pattern/format validation stays server-authoritative. |
| Global MCP elicitation/project UI | Verified | Pass 6 closes the client gap: connection/directory/workspace-owned canonical inventories and shared SSE; sessionless and chat access to native Project Requests. Close preserves drafts; submit/cancel advances. Input gating retains stop/interrupt/active-dictation controls. Scoped transport, hydration overlap/tombstones and prepaid refund regressions pass. Actual MCP external-URL completion notification remains untested; native adaptation, not exact web presentation parity. |
| Directory discovery and workspace navigation | Verified | Explicit resolution, clone/canonical identity, core session pagination, Files picker and destination cancellation/new chat. Older-UI regression now has workspace passing on both simulators; evidence recorded below. Manual browser is separate. Not arbitrary repository initialization. |
| Managed worktree copies | Verified | Exact next-17155 copy contract; explicit destination parent, dirty force decision and retained sessions tested on real server. No newer `/api/worktree` substitution. |
| Project actions/editor/chips | Implemented | Both profiles retain server sessions; owned-run hide/journal/recovery and full coordinator mock tests pass. Real paused command receipt passes; authorize and verify actual paid two-turn execution next. No HTTP/idle-only success or delete fallback. |
| Project appearance writes | Blocked | No verified supported next-17155 metadata writer contract; v2 read-only. Require a verified contract before advertising PATCH. |
| Destructive reset/archive, named-branch/startup setup | Blocked | Server-contract gaps; copy refresh is not reset and a copy name is not v1 branch/setup parity. Legacy reset service remains separate. |
| Git/files | Partial | Read/status/diff/search/raw content and workspace picker implemented; not general Git mutations. Verify real project workflows on device. |
| Terminal/PTY | Verified | HTTP/SSE/authenticated socket, resize/replay/exit and stale-response coverage. Historical terminal UI passed; hands-on terminal interactions on the installed pass 8 phone build remain unverified. |
| Providers, plugins, MCP status | Partial | Typed key/OAuth/credential flows, plugin metadata and MCP connect/disconnect implemented. Provider policy is narrower than session forms; personal OAuth/key writes untested. Custom-provider/config writer is unverified, not a confirmed absent server contract; external/command auth remains externally managed. |
| Canonical todos | Blocked | Next-17155 public runtime OpenAPI has no canonical todo endpoint/event; source research found only old migration references. Remain gated on this runtime; do not parse tool outputs or generalize absence to all future versions. |
| Foreground Talk/voice | Partial | Core creation/submission/admission, selection barrier and uncertain-input identity implemented. Window audio leases prevent an inactive window from deactivating another's audio. UI verifies chooser cancellation/availability without recording; physical audio ownership, ASR/TTS and provider-backed voice turns remain unverified. |
| Manual in-app browser | Verified | Independent of native bridge. WebViews/history are scoped by connection lifetime, project and directory; title KVO is browser-session-owned. One nonpersistent `WKWebsiteDataStore` shares cookies within a connection, not across reconnects. Unit and two UI tests cover history/collapse/reopen, project switching, reconnect privacy and no forwarded OpenCode auth. V2 bridge automation is explicitly deferred. |
| Native bridge/device tools | Deferred | User explicitly deferred OUR v2 `OpenClientPlugin`, native bridge and browser automation until OpenCode v2 is released and contracts settle. Do not implement the plugin now or count this as a current blocker. V2 supports plugins; the legacy function is incompatible with object `setup`/effect APIs. Manual browsing is independent. |
| Fun and Games / saved game actions | Implemented | Find the Bug (12 languages) and Find the Place use core default-directory resolution, session creation/selection and hidden setup with stable admission identity. Uncertainty reconciles reads; persisted profile-scoped rejection guards prevent metering bypass. Scripted English/Swift backend setup/answer smoke passes; native gameplay UI remains untested beyond chooser/cancel. |
| Widgets and widget actions | Implemented | V2 new-session and command actions use typed backend services and canonical target/catalog validation, no fallback or fabricated commands. Create-once checkpoints, exact admission, canonical precedence over late rejection even after ledger pruning, one-shot captured commerce/day reservations and no accepted-input refund. Same-owner settled completion retires handoff on reconnect; route errors are owned. UI proves new-draft cancel and missing-command handling, not valid-command execution. Read snapshots retain profile isolation and canonical-empty catalog behavior. |
| Shortcuts/App Intents | Partial | Pass 7 adds pagination beyond 100 and fresh canonical session/directory/workspace/root/project validation before mutation. Fixed resolved-profile operation hashes/locks preserve the original legacy pending-hash format. Typed short-lived services, no SSE, read reconciliation without reposting; locks remain process-local, not cross-process atomic. Real Shortcuts-host sends remain unverified. |
| Share handoff/composer | Partial | V2 ingestion enabled with exact saved configuration and destination validation, retained edited text/images and paywall draft, and session/selection checkpoints for explicit rejected-input retry. Uncertain admission is reconciled, not reposted. Only the owned stale routing error clears on successful revalidation. Cold/warm invalid-target and warm valid PNG preview/edit/revalidation/cancel UI tests pass; actual share-extension invocation and provider-backed send remain unverified. |
| Live Activities / background actions | Implemented | Profile/server/session/activity identity, v2 permission transport and typed-form Open App routing implemented; no inline `[[String]]` flattening. Missing profile is legacy; unknown fails closed. Attributes preserve project ID/canonical directory; legacy Global uses separate unscoped `requestDirectory`. Cold-tap path matches exact OS record, connects/joins saved server/profile, reads canonical session/pending permissions and guards after awaits. Same-owner reconnect reconciles rather than stopping all. Finite background assertion, no APNs. Real native ActivityKit start/retain/reconnect/stop UI passed both simulators; cold-process OS restore, Lock Screen and Dynamic Island remain unverified. |
| Detached chat windows | Implemented | Dedicated `ChatWindowContext` and real per-window facade isolate selection, drafts/attachments, focus/navigation/hydration, form editing, browser and errors while sharing canonical stores, connection, session model-configuration barrier and one event source. Scoped MCP/todo refresh and child contexts implemented; MCP retains compatibility coupling. Routes carry explicit profile/canonical directory/workspace; absent profile is legacy. Native iPad two-view root-draft preservation/child editing passed; iPhone action hidden. Primary app shells are not independent multiwindow copies; physical audio/keyboard and OS restoration unverified. |
| Persistent/offline cache | Partial | V2 read-through uses a separate namespace; legacy keys/schema unchanged. Historical UI proves transcript cache with HTTP 503 while discovery stays live, not offline navigation. Automatic-detection read-through/offline behavior remains unvalidated: without network/probe success the API profile is unknown, so no v1-cache read or silent legacy fallback is allowed. This known-profile boundary is intentional. |
| Physical-device baseline | Partial | Fresh `.derived-data-device` iphoneos build, strict signing and `devicectl` install of `com.ntoporcov.openclient` on Nic iPhone succeeded. Automatic-detection/cold-position build is NOW installed, superseding Thinking-sequence. Launch FAILED: phone locked. Unlock and open OpenClient manually; no hands-on validation claim. Log: `auto-detection-device-build.log` under the approved temporary opencode directory. |
| Release rollout flag | Deferred | Remote flag remains Track Later and is not required for the separately user-approved public automatic-only default. |

## Current Evidence

September 9, 2026, main-reported final source units: `cold-scroll-unit-final.xcresult`, **1,383 passed, 13 skipped, zero failed per device**. Known unrelated `ResponseTurnCaptionTests/testCaptionOccupiesSpaceOnlyWhileRevealed` is explicitly excluded, not passing. There are **1,396 reported outcomes plus one excluded = 1,397 including the exclusion**; skipped tests were not executed. This supersedes `auto-detection-final-unit-1` (1,382 passed, 13 skipped, one excluded) and the earlier 1,372-pass handoff. `auto-detection-final-ui-1` passed **three UI tests each**. No unqualified all-green claim.

The real-app initial-bottom token was consumed before collection bounds were usable. `ChatView` retains/retries the pending request after layout, marks the token handled only on success, and cancels on user drag. `cold-scroll-before` failed at **-54 versus expected 200**; `cold-scroll-after` passed **13 tests on each simulator**.

Full five-native-composer-turn acceptance passed on the **iPhone simulator** in `acceptance-next17155-gjs5t1i_/acceptance-ui.xcresult` and **iPad simulator** in `acceptance-next17155-rxm_tpkk/acceptance-ui.xcresult`. These are separate runs. Normal cold relaunch after the fix shows the latest canonical content with five user/five assistant messages, without test scrolling or refresh. The **final combined-run iPhone failed in Photo Picker, with no selected item and Done disabled**; do not claim a new clean combined matrix.

All fixture processes are reported stopped, with no personal-server or paid-provider calls. Manual widget execution, actual Share-host invocation and Shortcuts remain untested; the user checks Shortcuts. Main confirmed current localization lint **passed for 1,492 entries across seven catalogs**. The cold-position fix adds no copy; this documentation pass did not rerun lint.

Main confirmed the fresh `iphoneos` build in `.derived-data-device`, strict code-sign verification and `devicectl` installation of `com.ntoporcov.openclient` on Nic iPhone all succeeded. Log: `auto-detection-device-build.log` under the approved temporary opencode directory. The automatic-detection/cold-position build is now installed, superseding the Thinking-sequence baseline. **Launch failed because the phone was locked**; unlock and open OpenClient manually. Prior positive feedback does not validate the new build. This documentation pass makes no source/script changes and runs no tests, builds or server calls. No commit/push was requested; current user approval is required before committing.

### Historical Evidence: September 8, 2026

Main confirmed **1,290 passed, 13 skipped, 1,303 total per device** in `acceptance-part-end-unit-final.xcresult`, on fresh owned **iPhone 17 Pro Max and iPad Pro 13-inch (M5), iOS 26.3.1, stable Xcode 26.3**. Final core UI: **one passed, zero failures, zero skips per device** in `acceptance-next17155-binfvy2o/acceptance-ui.xcresult`, with `acceptance-ui-attachments` in that run directory under the approved temporary opencode root. Main localization: **1,490 keys across seven catalogs passed**.

Five native composer turns verify first/progress visible before provider finish, separate reasoning, Stop closing provider HTTP with canonical `aborted` and successful follow-up, a native Photos PNG with matching canonical/provider fingerprint and **6,247 bytes**, background completion/foreground catch-up without pull, and cold normal relaunch using saved credentials with **five user and five assistant messages, unique IDs**. Cold transcript relaunch does not prove uncertain-payload survival across app kill. This normal-mode test is next-17155 evidence, not a both-profile execution test.

Before fixture work, the real Legacy adapter regression reproduced `POST /session/.../prompt_async` returning 408, newer canonical live messages, then a GET missing the original input that left it at the tail as a normal message. Source/unit real-adapter mocked-408 and seeded root/dedicated-window UI now cover recovery in both profiles. No personal-server restart or physical-phone symptom-resolution evidence is claimed.

Acceptance found two production bugs: New Session awaited execution and hid streaming behind its sheet; it now returns after admission with a guarded completion-reconciliation task. Empty unfinished HTTP bodies erased live deltas; preservation now applies only to a matching unfinished live-owned part. Completed canonical parts win, including a completed part inside an ongoing message.

[Acceptance fixtures](scripts/acceptance/README.md) use actual next-17155 with a local scripted model: sandbox egress only to loopback 14097/14098, fresh private HOME/XDG/database/token per run, no paid-provider fallback, personal 4096/4097 mutations or user plugin. Earlier backend smoke passed **ten actual-server scenarios**, including command/game flows. This supersedes mock-only backend evidence, not the remaining **Safari Share actual-host and widget/game execution UI** gaps. Shortcuts-host verification is reserved for the user's manual check.

Main's fresh device build, strict signing and install succeeded; latest log is `submission-recovery-device-build.log` under the approved temporary opencode root. **Latest launch failed: phone locked.** Live Activity cold-process OS restoration, Lock Screen/Dynamic Island and physical background behavior remain unverified. This documentation update ran no tests/builds and made no app code, device or server changes. Concurrent response-copy work is separate and not attributed here.

### Pass 8 Historical Evidence: September 7, 2026

The pass-specific baseline statements below describe their historical handoffs, not the latest installed build.

Main confirmed the final matrix on **each iPhone 17 Pro Max and iPad Pro 13-inch (M5), iOS 26.3.1, Xcode 26.3**: `pass8-release-unit-1.xcresult` has **1,223 passed, 13 opt-in live skipped, 1,236 total**; `pass8-release-ui-1.xcresult` has **six passed, zero failures, zero skips**. No source fix was needed. These results supersede the earlier 1,216-pass review and `pass8-final-ui-3.xcresult` evidence.

UI coverage includes real native ActivityKit start/retain/reconnect/stop, widget new-draft cancellation/missing command, game chooser cancellation, and iPad dedicated-chat root-draft preservation/child editing (iPhone action hidden). Artifacts are under `/var/folders/v1/gzrsgbkd24b3l3dslnjtmv700000gq/T/opencode/`, including `pass8-release-ui-1-attachments`. This is not cold-process OS restoration, Lock Screen/Dynamic Island/background behavior, manual valid-command execution, actual gameplay or provider inference. Main's repeated localization check passed for **1,479 keys across seven catalogs**.

Main's fresh quiet `xcodebuild` for `iphoneos` in `.derived-data-device` succeeded; log: `/var/folders/v1/gzrsgbkd24b3l3dslnjtmv700000gq/T/opencode/pass8-device-build.log`. `codesign --verify --deep --strict` passed, followed by successful installation and launch of `com.ntoporcov.openclient` on Nic iPhone. **Pass 8 supersedes pass 7 as the confirmed phone baseline.** This documentation-only update ran no tests/builds and made no code, device or server changes.

### Pass 7 Historical Evidence: September 7, 2026

Main confirmed `pass7-final-unit` on **each iPhone 17 Pro Max and iPad Pro 13-inch (M5), iOS 26.3.1, Xcode 26.3**: **1,133 passed, 13 opt-in live skipped, 1,146 total**, after the action-source spelling correction from `session.input.delivered` to `session.input.promoted`. The earlier `pass7-cache-unit-fixed-1.xcresult` recorded the same counts before that final correction. Pass 7 localization passed for **1,476 entries across seven catalogs**.

`pass7-ui-final-2` recorded **five iPhone passes; four iPad passes and one cache failure**. After the production cache fixes, targeted `pass7-cache-ui-fixed-2` and `pass7-cache-ui-fixed-3` recorded **one cache UI pass on each destination across the reruns**. Widget, global-form, session-form and valid-Share selections had earlier passing evidence on both. These are separate runs, not one clean combined UI result. Cached UI retains live discovery and blocks transcript HTTP with 503; no provider inference or fully offline navigation is established.

Historical pass 7 device build/signing/install/launch succeeded; log: `/var/folders/v1/gzrsgbkd24b3l3dslnjtmv700000gq/T/opencode/pass7-device-build.log`. That baseline retained the legacy SSE fix and is now superseded by pass 8. Installation did not establish hands-on streaming symptom resolution or provider execution.

### Pass 6 Historical Evidence: September 6, 2026

Historical lead-confirmed pass 6 review: `pass6-review-unit-1.xcresult` has **1,076 passed, 13 opt-in live skipped, 1,089 total on each** iPhone 17 Pro Max and iPad Pro 13-inch (M5), iOS 26.3.1, Xcode 26.3. `pass6-review-ui-1.xcresult` has **two UI passes, zero skips on each**: new global forms (sessionless hydration, typed boolean `false`/integer answers, location SSE, next-request advancement, Close/draft retention and chat-blocking cancellation), plus the existing native session-form test. Artifact review used `pass6-review-ui-1-attachments`; both bundles and attachments were retained under the temporary artifact root below. Localization passed for **1,476 entries across seven catalogs**, including build extraction; this is not a fresh pass 7 localization count.

The review includes canonical-hydration rereads after refused store-revision replacement, overlapping default/explicit locations and settlement tombstones, plus refund of only the unowned prepaid allowance when a global request arrives before prompt POST. No actual MCP external-URL completion notification, real provider/voice authentication, or full UI-regression rerun is claimed; 13 opt-in live unit tests skipped. Disposable server 14097 is stopped and fixtures settled; persistent 4096/4097 were untouched.

The lead confirmed the fresh pass 6 `iphoneos` build completed, logged at `/var/folders/v1/gzrsgbkd24b3l3dslnjtmv700000gq/T/opencode/pass6-device-build.log`. `codesign --verify --deep --strict` succeeded; `devicectl` installed `com.ntoporcov.openclient` on Nic iPhone and process launch succeeded. That historical baseline included the legacy SSE fix, global forms and centralized Live Activity guard and is now superseded by pass 7. Launch alone does not verify hands-on MCP behavior or the user's legacy streaming symptom resolution.

### Pass 5 Historical Evidence: September 6, 2026

Historical lead-reported full unit/contract suite in `pass5-legacy-fix.xcresult`: **1,059 passed, 13 opt-in live skipped, 1,072 total per simulator**, on the same destinations. The legacy SSE fix remains included in pass 6. Two added `OpenCodeStreamingTests` cover the legacy batch timer delivering uncancelled callbacks and updating directory/active transcript without refresh. `OpenCodeManagedEventBatcher.flushScheduledEvents()` clears the timer handle before `flush()` so it cannot cancel itself and trigger backend event-discard guards. This is separate from the v2 input/inbox mismatch.

Pass 5 form rerun `pass5-form-picker-final-1.xcresult`: **one pass, zero skips per simulator**. The earlier `pass5-regression-final` recorded iPhone four passes and iPad three passes plus a form failure across proxy foreground refresh, cold relaunch, forms and workspace. The form rerun supersedes that failure; fixes concern test-harness full-viewport visibility and trailing-picker-label targeting, not production form changes. Foreground evidence is a test-only HTTP snapshot proxy, not actual-server message mutation.

Retained pass 5 artifacts available directly under `/var/folders/v1/gzrsgbkd24b3l3dslnjtmv700000gq/T/opencode/` are `pass5-legacy-fix.xcresult` and `pass5-form-picker-final-1.xcresult`. Earlier bundles/attachments were lost in host-restart temporary cleanup and are recorded historical evidence only. This documentation update did not run tests, build, or mutate servers.

Historical pre-restart evidence: **1,070 unit/contract tests and six new UI tests passed per simulator across `pass5-complete-1.xcresult` and `pass5-share-final-1.xcresult`, not one clean combined run.** The first run had five UI passes and a valid-Share preview-dismissal failure per destination; the final Share rerun passed on both, with no skips in either run. Warm Share used system-open URLs/same-PID assertions and seeded payload storage, not an actual share-extension invocation. Talk coverage was availability/cancel without recording; two browser tests covered manual browsing/isolation. Pass 4's 995 unit/contracts, three UI passes and phone installation remain historical.

The lead confirmed the fresh `.derived-data-device` `iphoneos` build succeeded, with log `/var/folders/v1/gzrsgbkd24b3l3dslnjtmv700000gq/T/opencode/pass5-device-build.log`. `codesign --verify --deep --strict` succeeded; `devicectl` installed `com.ntoporcov.openclient` on Nic iPhone and process launch succeeded. That historical pass 5 baseline included the legacy batch-timer fix and is superseded by pass 6. Localization lint passed for 1,472 entries across seven catalogs.

The completed pass 7 unit suite skipped 13 opt-in live tests; do not claim all earlier live contracts or UI were rerun. Historical isolated server 14097 discovery/create/model-selection tests sent no prompts. User confirmation of actual on-phone legacy streaming remains pending, with no visual symptom-resolution claim from install/launch. Still owed: hands-on terminal interactions, actual voice ASR/TTS, real Share extension invocation, Shortcuts-host sends, personal provider OAuth/key writes, paid two-turn action execution and provider-generated assistant streaming. Pass 7 event-contract fixtures do not establish inference.

## Next Priority

1. Unlock Nic iPhone and open the now-installed OpenClient build manually; automatic launch failed because the phone was locked. Verify normal automatic connection, native notice dismissal and cold positioning on phone without a public API picker or forced-v2 flow. Keep the known caption exclusion and final combined-run Photo Picker failure explicit despite separate passing core runs; prior Thinking-sequence feedback does not validate this build.
2. Verify cold-process Live Activity OS restoration and Lock Screen/Dynamic Island/background behavior, then physical multiwindow audio/keyboard and OS restoration. Implemented identity, transport and dedicated-window state are not stale implementation gates.
3. Verify Safari Share from the actual host and native widget/game execution UI; scripted backend smoke is not host/UI execution. The user will check Shortcuts manually. Paid inference/actions, ASR/TTS, personal OAuth/key writes and MCP external completion remain unverified; normal composer scripted streaming/interruption now passes on both simulators.
4. Keep next-17155 canonical todos blocked; source/runtime input promotion is already mapped. Track unavailable verified custom-provider/configuration writer and metadata/destructive reset/archive/branch/setup contracts separately. Fully offline navigation remains outside bounded read-through caching. Typed inline Live Activity forms are unavailable; Open App is intentional, not a legacy-answer fallback.
5. Respect the explicit plugin/native-bridge/browser-automation deferral until OpenCode v2 release/contracts settle. No second target or all-harness independence is claimed, including window MCP compatibility. Do not expand Git/repository initialization without v1 proof. Remote rollout flag remains Track Later, independently of the approved public automatic-only default; no full v1 parity claim.
